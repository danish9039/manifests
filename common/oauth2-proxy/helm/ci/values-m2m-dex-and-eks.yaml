credentials:
  clientSecret: not-a-real-client-secret
  cookieSecret: not-a-real-cookie-secret-00000

istioExternalAuth:
  enabled: true

m2m:
  enabled: true
  issuer: https://oidc.eks.AWS_REGION.amazonaws.com/id/CLUSTER_ID

clusterJwksProxy:
  enabled: false

parameters:
  forceHttps: "true"
