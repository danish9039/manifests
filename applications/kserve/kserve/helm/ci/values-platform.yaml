# CI values for the KServe platform scenario. Mirrors the defaults explicitly
# so the compared scenario, and the last installation revision, do not depend
# on chart defaults.
payload:
  scenario: platform
  customResourceDefinitions:
    enabled: true
  resources:
    enabled: true
  clusterServingRuntimes:
    enabled: true
