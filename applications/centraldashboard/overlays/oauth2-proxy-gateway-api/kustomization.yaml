apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# Using kserve-gateway-api overlay because it's used by example installation and
# keeps VirtualService + optional Gateway API route resources together.
- ../../upstream/overlays/kserve-gateway-api

components:
- ../../../../common/oauth2-proxy/components/central-dashboard
